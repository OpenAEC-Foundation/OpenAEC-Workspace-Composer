import { GpuServerPage } from "./GpuServerPage";

// Folder Sync is part of the GPU Server feature — reuse the component
// but focused on the sync session manager section
export function SyncPage() {
  return <GpuServerPage />;
}
